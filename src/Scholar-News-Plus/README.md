# Scholar-News-Plus

[![Python 3.8+](https://img.shields.io/badge/python-3.8%2B-blue.svg)](https://www.python.org/downloads/)
[![MIT License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

一款 **自动化学术信息跟踪工具**，支持多源文献爬取、AI智能分析（聚类/总结）、最新AI新闻获取，并生成可视化HTML报告，帮助研究者高效跟踪领域动态。


## 🌟 核心功能
| 功能模块          | 具体能力                                                                 |
|-------------------|--------------------------------------------------------------------------|
| 多源文献爬取      | 支持 Arxiv、PubMed、bioRxiv、Google Scholar（需代理）四大学术平台        |
| 智能文献处理      | 自动去重（按标题）、LLM主题聚类（通义千问）、关键词/创新点提取           |
| 实时AI新闻获取    | 通过Coze API获取最新10条AI领域新闻，无需手动整理                          |
| 可视化报告生成    | 输出HTML格式报告，包含文献主题分类、作者/摘要/创新点，支持浏览器直接打开 |
| 灵活配置          | 自定义关键词、数据源、爬取数量，配置文件自动生成，新手友好                |
| 运行日志保存      | 自动存储运行状态日志，便于后续回溯和分析                                  |


## 🚀 快速开始

### 1. 环境准备
- **Python版本**：3.8 及以上（推荐3.10，兼容性最佳）
- **依赖管理**：使用`pip`安装依赖，或用`conda`创建独立环境（推荐）

### 2. 安装步骤
#### 步骤1：克隆仓库（或直接下载代码）
```bash
# 克隆仓库（若使用Git）
git clone https://github.com/your-username/scholar-news-plus.git
cd scholar-news-plus

# 若不使用Git，直接下载代码压缩包并解压，进入解压目录
```

#### 步骤2：安装依赖
```bash
# 安装所有依赖（包含爬取、LLM、可视化相关库）
pip install -r requirements.txt
```

**`requirements.txt`内容**（若手动安装，可复制以下内容创建文件）：
```txt
requests>=2.31.0
beautifulsoup4>=4.12.2
tqdm>=4.66.1
markdown>=3.5.1
openai>=1.13.3
scholarly>=1.7.11
arxiv>=2.1.0
metapub>=0.5.2
biopython>=1.83
cozepy>=0.1.7
python-dotenv>=1.0.1  # 可选，用于管理环境变量
```

### 3. 配置关键参数
首次运行脚本会自动生成`scholar_daily_config.json`配置文件，需补充以下**必填参数**（否则部分功能无法使用）：

#### 3.1 通义千问API密钥（用于文献聚类/总结）
1. 前往 [阿里云DashScope平台](https://dashscope.aliyun.com/) 注册账号并申请API密钥（免费配额足够测试使用）
2. 打开`scholar_daily_config.json`，替换`DASHSCOPE_API_KEY`字段：
   ```json
   {
     "Keywords": ["artificial intelligence", "machine learning"],
     "Sources": ["Arxiv", "PubMed"],
     "max_num_per_source": 5,
     "DASHSCOPE_API_KEY": "your-dashscope-api-key-here",  // 替换为你的密钥
     "Entrez_email": "your-email@example.com",  // 替换为你的邮箱（PubMed必填）
     "log_path": "./scholar_logs"
   }
   ```

#### 3.2 PubMed邮箱（避免访问被拒）
PubMed要求填写邮箱才能使用API，直接在`Entrez_email`字段填入你的常用邮箱（如校园邮箱、个人邮箱）。

#### 3.3 Coze API密钥（用于获取AI新闻）
1. 前往 [Coze平台](https://www.coze.cn/) 注册账号，创建机器人并获取API密钥
2. 设置环境变量（避免硬编码密钥，更安全）：
   - **Windows**（命令行）：
     ```cmd
     set COZE_API_TOKEN=your-coze-api-key-here
     ```
   - **Mac/Linux**（终端）：
     ```bash
     export COZE_API_TOKEN=your-coze-api-key-here
     ```


### 4. 运行脚本
#### 方式1：默认配置运行（爬取昨天的文献）
直接执行主脚本，工具会按默认配置（关键词、数据源）运行：
```bash
python main.py  # 假设主代码文件名为main.py（若不同，替换为实际文件名）
```

#### 方式2：自定义配置（推荐）
在`main.py`的`if __name__ == "__main__":`部分修改参数，示例：
```python
if __name__ == "__main__":
    # 1. 初始化实例
    scholar = ScholarDaily()

    # 2. 自定义关键词（跟踪特定领域，如LLM微调）
    scholar.set_keywords(["large language model", "LLM fine-tuning", "parameter-efficient tuning"])

    # 3. 自定义数据源（可选：Arxiv, PubMed, bioRxiv, GoogleScholar）
    scholar.set_sources(["Arxiv", "bioRxiv"])  # 排除Google Scholar（无需代理）

    # 4. 自定义目标日期（格式：YYYYMMDD，默认爬取昨天）
    scholar.date = datetime.strptime("20240610", "%Y%m%d").date()

    # 5. 运行完整流程
    scholar.run()
```

#### 运行成功标志
```
=== ScholarDaily 开始运行（目标日期：2024-06-10）===

=== 开始搜索文献 ===

--- 正在搜索 Arxiv ---
query condition as:
 all:large language model OR all:LLM fine-tuning OR all:parameter-efficient tuning
100%|██████████| 5/5 [00:03<00:00,  1.52it/s]
Arxiv 找到 3 篇文献

--- 正在搜索 bioRxiv ---
query url as:
 https://www.biorxiv.org/search/abstract_title%3A%22large%20language%20model%22%20OR%20%22LLM%20fine-tuning%22%20OR%20%22parameter-efficient%20tuning%22%20abstract_title_flags%3Amatch-phrase%20numresults%3A5%20sort%3Apublication-date%20direction%3Adescending
100%|██████████| 5/5 [00:12<00:00,  2.45s/it]
bioRxiv 找到 2 篇文献

=== 开始处理文献 ===
去重后剩余 5 篇文献
成功聚类：5 篇文献 → 2 个主题
100%|██████████| 5/5 [00:15<00:00,  3.02s/it]
=== 文献处理完成 ===

=== 开始获取AI新闻 ===
=== AI新闻获取完成 ===

=== 开始生成报告 ===
报告已保存：/path/to/scholar-news-plus/ScholarDaily_20240610.html
日志已保存：/path/to/scholar-news-plus/scholar_logs/ScholarDaily_20240610.pkl

=== ScholarDaily 运行完成 ===
```


## 📂 项目目录结构
```
scholar-news-plus/
├── main.py                # 主脚本（包含ScholarDaily类和运行逻辑）
├── scholar_daily_config.json  # 配置文件（自动生成，需补充API密钥）
├── requirements.txt       # 依赖列表
├── ScholarDaily_20240610.html  # 生成的HTML报告（按日期命名）
├── scholar_logs/          # 日志目录（存储pickle格式运行日志）
│   └── ScholarDaily_20240610.pkl
└── README.md              # 项目说明（本文档）
```


## ⚠️ 常见问题（FAQ）
### Q1：Google Scholar爬取失败/无结果？
A1：Google Scholar对国内IP限制严格，且易触发反爬，解决方案：
- 使用代理工具（如VPN），确保全局代理生效
- 减少`max_num_per_source`（建议≤3），增加`time.sleep`时长（在`search_GoogleScholar`方法中调整）
- 优先使用Arxiv/PubMed等无限制平台

### Q2：通义千问API报错“Invalid API Key”？
A2：
- 检查`DASHSCOPE_API_KEY`是否填写正确（无多余空格）
- 前往DashScope平台确认API密钥未过期，且有剩余配额
- 若使用免费配额，避免短时间内频繁调用（建议每篇文献间隔≥2秒）

### Q3：PubMed提示“API rate limit exceeded”？
A3：
- 确保`Entrez_email`已填写（PubMed通过邮箱识别用户，避免限速）
- 减少爬取数量，或增加`time.sleep`（在`search_PubMed`方法中添加`time.sleep(1)`）

### Q4：Coze新闻获取失败“Token not found”？
A4：
- 检查环境变量`COZE_API_TOKEN`是否设置正确（Windows需重新打开命令行生效）
- 确认Coze API密钥未过期，且机器人ID（`bot_id=7446278637571670025`）有效（可替换为自己创建的机器人ID）


## 🛠️ 扩展建议
1. **新增数据源**：如需支持IEEE Xplore、Springer等平台，可参考现有`search_*`方法，添加新的搜索函数并更新`function_mapping`
2. **更换LLM模型**：若需使用GPT-4o或豆包，修改`_cluster_by_topic`和`_summarize_papers`中的`OpenAI`客户端配置（调整`base_url`和`model`）
3. **定时运行**：在Linux下用`crontab`、Windows下用“任务计划程序”设置每日自动运行，实现“每日文献日报”
4. **邮件推送**：扩展`construct_html_report`方法，添加SMTP邮件发送功能，将报告自动发送到邮箱


## 📄 许可证
本项目基于 [MIT许可证](LICENSE) 开源，你可以自由使用、修改和分发，无需商业授权，但需保留原作者版权信息。


## 🤝 贡献指南
欢迎通过以下方式参与项目：
1. 提交Issue：反馈bug或提出新功能建议
2. 提交PR：修复bug、优化代码或新增功能（建议先在Issue中沟通方案）
3. 分享经验：在GitHub Discussions中分享你的使用技巧或扩展方案

所有贡献需遵循[代码规范](https://www.python.org/dev/peps/pep-0008/)，确保代码可读性和兼容性。